#include <stdio.h>
#include <math.h>

int main()
{
    char alpha;

    for (alpha = 'A'; alpha <= 'Z'; ++alpha)
    {
        printf("the alphabet letter %c, would be the %d. \n", alpha, alpha);
    }
}
